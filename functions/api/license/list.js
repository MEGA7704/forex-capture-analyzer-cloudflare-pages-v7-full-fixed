import { json, options, requireAdmin, normalizeKey, getLicenses, sanitizeLicenseForClient } from '../../_lib/common.js'; export async function onRequest({request,env}){ if(request.method==='OPTIONS') return options(); try{ requireAdmin(request,env); const url=new URL(request.url); const q=normalizeKey(url.searchParams.get('q')||''); const status=String(url.searchParams.get('status')||'').trim(); const plan=String(url.searchParams.get('plan')||'').trim(); const limit=Math.min(Number(url.searchParams.get('limit')||200),1000); let items=await getLicenses(env,request); if(q) items=items.filter(x=>normalizeKey(x.license_key).includes(q)||normalizeKey(x.device_id||'').includes(q)); if(status) items=items.filter(x=>String(x.status||'')===status); if(plan) items=items.filter(x=>String(x.plan_id||'')===plan); return json({ok:true,items:items.slice(0,limit).map(sanitizeLicenseForClient)});}catch(err){ return json({ok:false,error:err.message},401);} }
